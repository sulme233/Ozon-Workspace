# 2026-04-15

- Ran full project validation: `check-config`, all unit tests, `daily`, and `dashboard`.
- Added store-level parallel execution (`--max-workers`) to daily/dashboard pipelines to reduce full-run wall time.
- Fixed missing imports for `get_store_identity` in orders/pricing/sales pipelines.
- Improved orders robustness: unfulfilled-postings API failures now downgrade to partial with warnings instead of breaking the whole module.
- Improved ads robustness: retry previous-day range when the API rejects same-day stats as future interval.
- Updated FBS postings request builder to omit empty status filter and normalize status values.
- Added/expanded tests for daily parallel merge, orders fallback behavior, and FBS status filter behavior.
- Added compact output mode for daily/dashboard by default; introduced `--include-details` switch to keep full payloads when needed.
- Verified compact mode removes heavy keys (`ads.detail`, `sku_risk.sku_risks`) and keeps previews used by dashboard UI.
- Added `.gitignore` for Python cache/venv and dashboard history runtime artifacts.
- Added dashboard `--no-history` option; can skip writing history snapshots during refresh/serve.
- Added SQLite persistence module `ozon_db.py` and wired dashboard refresh to store snapshots and store-level metrics.
- Extended dashboard backend API with `/api/health`, `/api/snapshots`, `/api/snapshots/latest`, `/api/stores/trend`, `/api/ozon-api/catalog`.
- Added Ozon API catalog module `ozon_api_catalog.py` and API reference map docs.
- Extended frontend `dashboard/app.js` with backend diagnostics panels (snapshot history, store trend, API coverage).
- Completed live serve-mode interface checks (health/refresh/snapshots/latest/trend/catalog all passed).
- Added unified smoke test script `scripts/smoke_test_interfaces.py` and CLI commands `run_ozon.py smoke` / `run_ozon.py api-smoke`.
- Smoke test now validates local dashboard APIs and optionally probes read-only Ozon endpoints (`--probe-ozon`).
- Added smoke-related tests: `test_smoke_interfaces.py` and `test_run_ozon_cli.py`; full suite now runs 29 tests.
- Real smoke verification succeeded for local APIs and live Ozon probes (`perf_campaigns`, `product_prices`, `fbs_postings`); refresh endpoint can be skipped or set strict via `--skip-refresh` / `--strict-refresh`.
